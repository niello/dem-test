
function Update(ActorID, SOID)

	--if all entities are in transition zone or are destroyed, return Success if at least 1 entity is alive, else fail
	--return running
	--can periodically re-warn user, or do it only on start?
	
	--all entities - all entities of that faction on a far transition
	--for near transitions each of selected entities transits independently by using this action
	
	--!!!DBG TMP!
	return 1 --Success

end
